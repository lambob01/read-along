PUBLIC_ABS_ORIGIN=https://your-abs-server.com
# Local HTTP proxy (e.g. Clash Verge's mixed port) for the dev server's own
# connection to ABS, when that host is only reachable through a VPN.
# ABS_HTTP_PROXY=http://127.0.0.1:7897
